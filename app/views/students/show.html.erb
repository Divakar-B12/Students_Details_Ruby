<style>
  .notice-msg {
    color: #0f5132;
    background: #d1e7dd;
    padding: 10px 14px;
    border-radius: 6px;
    font-weight: 600;
    display: inline-block;
    margin-bottom: 20px;
  }

  .student-show-wrapper {
    width: 420px;
    margin: 30px auto;
    padding: 25px;
    border-radius: 12px;
    background: linear-gradient(145deg, #f0f0f0, #ffffff);
    box-shadow:
      6px 6px 12px #cfcfcf,
      -6px -6px 12px #ffffff;
    transition: all 0.3s ease;
  }

  .student-show-wrapper:hover {
    transform: translateY(-6px);
    box-shadow:
      10px 10px 20px #c5c5c5,
      -10px -10px 20px #ffffff;
  }

  .student-actions {
    margin-top: 20px;
    text-align: center;
  }

  .student-actions a {
    text-decoration: none;
    color: #4a90e2;
    border: 1px solid #4a90e2;
    padding: 6px 14px;
    border-radius: 6px;
    margin: 0 6px;
    font-weight: bold;
    transition: all 0.3s ease;
  }

  .student-actions a:hover {
    background-color: #4a90e2;
    color: white;
    transform: scale(1.05);
  }

  /* Existing destroy button base style */
  .destroy-btn input {
    background-color: #ff4d4f;
    color: white;
    border: none;
    padding: 8px 16px;
    border-radius: 6px;
    cursor: pointer;
    margin-top: 12px;
    transition: all 0.3s ease;
  }

  .destroy-btn input:hover {
    background-color: #d9363e;
    transform: scale(1.05);
  }

  /* Enhanced 3D effect only for destroy button */
  .destroy-btn {
    display: inline-block;
    perspective: 1000px;
  }

  .destroy-btn input {
    background: linear-gradient(145deg, #ff4d4f, #d9363e);
    box-shadow:
      4px 4px 8px #a83232,
      -4px -4px 8px #ff7b7b;
    font-weight: bold;
    letter-spacing: 0.5px;
  }

  .destroy-btn input:hover {
    background: linear-gradient(145deg, #d9363e, #ff4d4f);
    box-shadow:
      6px 6px 12px #a83232,
      -6px -6px 12px #ff7b7b;
    transform: scale(1.08) rotateX(5deg);
  }

  .destroy-btn input:active {
    transform: scale(0.95);
    box-shadow:
      inset 3px 3px 6px #a83232,
      inset -3px -3px 6px #ff7b7b;
  }
</style>

<p class="notice-msg"><%= notice %></p>

<div class="student-show-wrapper">
  <%= render @student %>

  <div class="student-actions">
    <%= link_to "Edit this student", edit_student_path(@student) %>
    <%= link_to "Back to students", students_path %>

    <div class="destroy-btn">
      <%= button_to "Destroy this student", @student, method: :delete %>
    </div>
  </div>
</div>
